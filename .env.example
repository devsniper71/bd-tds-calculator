# Google Analytics 4 Measurement ID. Get it from your GA4 property:
#   Admin → Data streams → your web stream → "Measurement ID" (looks like G-XXXXXXXXXX)
# Leave unset (locally / in forks) to disable analytics entirely.
NEXT_PUBLIC_GA_ID=
