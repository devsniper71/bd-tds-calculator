# @vercel/analytics declares an *optional* peer on @sveltejs/kit, which npm's
# strict peer resolver tries to satisfy and ends up conflicting with the
# vite@5 that vitest pins. We don't use Svelte, so relax peer resolution.
# Set here (not just on the CLI) so `npm ci` on Vercel resolves identically.
legacy-peer-deps=true
